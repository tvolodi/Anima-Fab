/**
 * S02-local color additions.
 *
 * core/theme/colors.ts carries ep01's dramatic vocabulary (VERIFIED_BORDER,
 * WRONGLY_BLAMED, GAP_HIGHLIGHT) - those encode specific narrative claims
 * (audited vs. not, blame, the absent manager) that don't apply to this
 * episode. Rather than reuse those constants for an unrelated purpose here,
 * this file defines S02's own semantic color: the single accent used to mark
 * "the one non-obvious thing to look at" (the lifecycle's closing arc, the
 * classification dots landing together). If a second S02 episode wants the
 * same accent, promote it to core then - not before.
 */

export const EMPHASIS = "#F2C14E";
